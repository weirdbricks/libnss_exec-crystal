name: libnss_exec
version: 3.0.0

authors:
  - Tyler Akins (original C implementation)

description: |
  NSS module that delegates passwd/group/shadow lookups to an external script.
  Crystal port of https://github.com/tests-always-included/libnss_exec
  Uses raw C library calls only — no GC, no Crystal runtime dependencies.
  Safe for dlopen() loading by glibc.

crystal: ">= 1.0.0"
license: MIT

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.6
